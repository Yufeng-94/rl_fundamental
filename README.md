# The Repo is Work in Progress...

# Create Python Environment

All dependencies are listed in `pyproject.toml`. The environment can be created using [poetry](https://python-poetry.org/) or other tools. If using poetry, run:

```bash
poetry lock
poetry install
eval $(poetry env activate)
```

